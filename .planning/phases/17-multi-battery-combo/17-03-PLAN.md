---
phase: 17-multi-battery-combo
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/components/calculations/wizard/steps/battery-step.tsx
autonomous: true

must_haves:
  truths:
    - "Closer can adjust quantity for each battery (COMBO-01)"
    - "Closer can toggle between Komboinvestering and Jamfora modes (COMBO-02)"
    - "Mode toggle visible when batteries are added"
    - "Quantity selector shows +/- controls"
  artifacts:
    - path: "src/components/calculations/wizard/steps/battery-step.tsx"
      provides: "Battery step with quantity selector and mode toggle"
      contains: "comboMode"
  key_links:
    - from: "src/components/calculations/wizard/steps/battery-step.tsx"
      to: "src/stores/calculation-wizard-store.ts"
      via: "uses updateBatteryQuantity and setComboMode actions"
      pattern: "updateBatteryQuantity|setComboMode"
---

<objective>
Extend the battery step UI with quantity selector per battery and mode toggle (Komboinvestering/Jamfora).

Purpose: Enable closers to configure multi-battery investments through the wizard UI.
Output: Updated battery-step.tsx with quantity controls and mode toggle.
</objective>

<execution_context>
@/Users/julian.nordgren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julian.nordgren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-multi-battery-combo/17-CONTEXT.md
@.planning/phases/17-multi-battery-combo/17-RESEARCH.md
@.planning/phases/17-multi-battery-combo/17-01-SUMMARY.md

@src/components/calculations/wizard/steps/battery-step.tsx
@src/stores/calculation-wizard-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode toggle to battery step header</name>
  <files>src/components/calculations/wizard/steps/battery-step.tsx</files>
  <action>
1. Import comboMode state and setComboMode action from the store:
```typescript
const {
  batteries,
  addBattery,
  removeBattery,
  updateBatteryPricing,
  updateBatteryQuantity,  // New from 17-01
  comboMode,              // New from 17-01
  setComboMode,           // New from 17-01
} = useCalculationWizardStore()
```

2. Add mode toggle UI below the page heading, visible when at least 1 battery is added:
```tsx
{batteries.length > 0 && (
  <div className="mb-6">
    <label className="block text-sm font-medium text-gray-700 mb-2">
      Visningsläge
    </label>
    <div className="flex bg-gray-100 rounded-lg p-1 max-w-md">
      <button
        type="button"
        onClick={() => setComboMode('komboinvestering')}
        className={`flex-1 py-2 px-4 rounded-md text-sm font-medium transition-colors ${
          comboMode === 'komboinvestering'
            ? 'bg-white shadow text-gray-900'
            : 'text-gray-600 hover:text-gray-900'
        }`}
      >
        Komboinvestering
      </button>
      <button
        type="button"
        onClick={() => setComboMode('jamfora')}
        className={`flex-1 py-2 px-4 rounded-md text-sm font-medium transition-colors ${
          comboMode === 'jamfora'
            ? 'bg-white shadow text-gray-900'
            : 'text-gray-600 hover:text-gray-900'
        }`}
      >
        Jämför
      </button>
    </div>
    <p className="text-xs text-gray-500 mt-1">
      {comboMode === 'komboinvestering'
        ? 'Kombinerar alla batterier till en total investering'
        : 'Jämför batterier sida vid sida'}
    </p>
  </div>
)}
```

3. Place the toggle after the heading and description, before the battery selector dropdown.

4. Per CONTEXT.md decision: Default mode is Komboinvestering (already set in store).
  </action>
  <verify>
- Mode toggle renders when batteries.length > 0
- Clicking toggles between modes
- Visual feedback shows active mode
- grep confirms: `grep -n "comboMode" src/components/calculations/wizard/steps/battery-step.tsx`
  </verify>
  <done>
- Mode toggle visible after adding first battery
- Komboinvestering and Jamfora buttons work
- Active mode has visual highlight (white bg, shadow)
- Helper text explains each mode
  </done>
</task>

<task type="auto">
  <name>Task 2: Add quantity selector to each battery card</name>
  <files>src/components/calculations/wizard/steps/battery-step.tsx</files>
  <action>
1. Update the battery card render to include a quantity selector. Add this below the battery name/specs, before the pricing inputs:

```tsx
{/* Quantity selector */}
<div className="mb-4">
  <label className="block text-sm font-medium text-gray-700 mb-1">
    Antal enheter
  </label>
  <div className="flex items-center gap-2">
    <button
      type="button"
      onClick={() => updateBatteryQuantity(index, Math.max(1, (selected.quantity ?? 1) - 1))}
      disabled={(selected.quantity ?? 1) <= 1}
      className="px-3 py-1.5 border border-gray-300 rounded-md hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed"
    >
      -
    </button>
    <span className="w-12 text-center font-medium text-lg">
      {selected.quantity ?? 1}
    </span>
    <button
      type="button"
      onClick={() => updateBatteryQuantity(index, (selected.quantity ?? 1) + 1)}
      className="px-3 py-1.5 border border-gray-300 rounded-md hover:bg-gray-50"
    >
      +
    </button>
  </div>
</div>
```

2. Update the price summary section to show per-unit AND total when quantity > 1:
```tsx
{selected.totalPriceExVat > 0 && (
  <div className="mt-4 p-3 bg-gray-50 rounded-md">
    <div className="grid grid-cols-2 gap-2 text-sm">
      {/* Per-unit prices */}
      <div className="flex justify-between">
        <span className="text-gray-600">Per enhet ex. moms:</span>
        <span className="font-medium">
          {formatSek(selected.totalPriceExVat + selected.installationCost)}
        </span>
      </div>

      {/* Show totals if quantity > 1 */}
      {(selected.quantity ?? 1) > 1 && (
        <>
          <div className="flex justify-between col-span-2 pt-2 border-t">
            <span className="text-gray-600">
              Totalt för {selected.quantity} enheter ex. moms:
            </span>
            <span className="font-medium">
              {formatSek((selected.totalPriceExVat + selected.installationCost) * (selected.quantity ?? 1))}
            </span>
          </div>
          <div className="flex justify-between col-span-2">
            <span className="text-gray-600">
              Totalt inkl. moms (25%):
            </span>
            <span className="font-medium">
              {formatSek((selected.totalPriceExVat + selected.installationCost) * (selected.quantity ?? 1) * 1.25)}
            </span>
          </div>
        </>
      )}

      {/* After Gron Teknik - always show total */}
      <div className="flex justify-between col-span-2 pt-2 border-t">
        <span className="text-gray-600">
          Efter Grön Teknik (48.5%):
        </span>
        <span className="font-medium text-green-600">
          {formatSek(
            (selected.totalPriceExVat + selected.installationCost) *
            (selected.quantity ?? 1) * 1.25 * (1 - 0.485)
          )}
        </span>
      </div>
    </div>

    {/* ... existing margin info for ProffsKontakt ... */}
  </div>
)}
```

3. Update the comparison hint text at the bottom:
```tsx
{batteries.length > 0 && (
  <p className="text-sm text-blue-600 mt-4">
    {comboMode === 'komboinvestering'
      ? `${batteries.reduce((sum, b) => sum + (b.quantity ?? 1), 0)} batterier totalt - kombinerad investering visas i resultatsteget.`
      : `${batteries.length} batterikonfiguration(er) valda - jämförelse visas i resultatsteget.`}
  </p>
)}
```

4. Remove the max 4 battery limit OR update it based on mode:
- In Jamfora mode: Keep max 4 (or 3 per COMBO-10)
- In Komboinvestering mode: No limit (per CONTEXT.md decision)

Update handleAddBattery and the disabled states:
```typescript
// Max batteries depends on mode
const maxBatteries = comboMode === 'jamfora' ? 3 : 10 // Reasonable limit for combo
```
  </action>
  <verify>
- Quantity selector appears for each battery
- +/- buttons work correctly
- Minimum quantity is 1 (can't go below)
- Price summary shows per-unit and total when quantity > 1
- Gron Teknik calculated on total (not per-unit)
  </verify>
  <done>
- Quantity selector with +/- buttons on each battery card
- Price summary adapts to show totals when quantity > 1
- Total count in hint text reflects quantities
- Max batteries limit adjusted per mode
  </done>
</task>

<task type="auto">
  <name>Task 3: Allow adding same battery multiple times OR different models</name>
  <files>src/components/calculations/wizard/steps/battery-step.tsx</files>
  <action>
1. Currently, the code prevents adding the same battery twice:
```typescript
// Check if already added
if (batteries.some(b => b.configId === selectedBatteryId)) {
  return
}
```

2. For Komboinvestering mode, users should use the quantity selector instead of adding duplicates. Keep this check.

3. For Jamfora mode, users might want to compare the same model at different prices. HOWEVER, per CONTEXT.md, quantity selector is the preferred way. Keep the duplicate prevention.

4. Update the dropdown filter to still show batteries that are already added (so users know what's selected), but disable them:
```tsx
{batteryList.map((b) => {
  const isSelected = batteries.some(sel => sel.configId === b.id)
  return (
    <option
      key={b.id}
      value={b.id}
      disabled={isSelected}
    >
      {b.brandName} {b.name} - {b.capacityKwh} kWh
      {isSelected ? ' (redan vald)' : ''}
    </option>
  )
})}
```

5. Make sure the "Lägg till" button validates correctly.
  </action>
  <verify>
- Already-selected batteries show "(redan vald)" in dropdown
- Cannot add same battery config twice
- Quantity selector is the way to add more of same model
- Different batteries can be added side by side
  </verify>
  <done>
- Dropdown shows all batteries, disabled if already selected
- Duplicate prevention works
- Multiple different models can be added
- UX guides toward quantity selector for same-model quantities
  </done>
</task>

</tasks>

<verification>
1. Mode toggle visible after adding battery: Start wizard, add battery, see toggle
2. Default mode is Komboinvestering
3. Quantity selector works: Click +, see quantity increase
4. Price summary shows totals when quantity > 1
5. TypeScript compiles: `npx tsc --noEmit`
</verification>

<success_criteria>
- Mode toggle (Komboinvestering/Jamfora) visible when batteries added
- Default mode is Komboinvestering (per CONTEXT.md)
- Quantity selector (+/-) on each battery card
- Quantity minimum is 1
- Price summary shows per-unit and total when quantity > 1
- Gron Teknik applied to total investment
- Hint text shows total battery count (considering quantities)
- Max 3 batteries in Jamfora mode (COMBO-10)
</success_criteria>

<output>
After completion, create `.planning/phases/17-multi-battery-combo/17-03-SUMMARY.md`
</output>
