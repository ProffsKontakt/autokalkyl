---
phase: 13-bug-fixes-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils.ts
  - src/actions/share.ts
  - src/components/calculations/breakdowns/spotpris-breakdown.tsx
autonomous: true

must_haves:
  truths:
    - "Spotpris breakdown shows efficiency as percentage (90.2% not 90000.2%)"
    - "All percentages in breakdown display with max 2 decimals, no trailing zeros"
    - "Daglig energi shows kWh with 2 decimals"
    - "Daglig besparing shows SEK with 2 decimals"
  artifacts:
    - path: "src/lib/utils.ts"
      provides: "formatPercentage utility function"
      exports: ["formatPercentage"]
      contains: "parseFloat.*toFixed"
    - path: "src/actions/share.ts"
      provides: "Fixed efficiency calculation (divided by 100)"
      contains: "efficiency.*100"
    - path: "src/components/calculations/breakdowns/spotpris-breakdown.tsx"
      provides: "Fixed percentage display using formatPercentage"
      contains: "formatPercentage"
  key_links:
    - from: "src/components/calculations/breakdowns/spotpris-breakdown.tsx"
      to: "src/lib/utils.ts"
      via: "import formatPercentage"
      pattern: "import.*formatPercentage.*from.*utils"
---

<objective>
Fix Spotpris efficiency display bug (FIX-01) and breakdown value formatting (FIX-04).

Purpose: The efficiency value displays as 90000.2% instead of 90.2% because the efficiency stored as percentage (95) is multiplied incorrectly. Also updates breakdown formatting to use consistent Swedish labels and decimal precision.

Output: Working spotpris breakdown with correct percentage display (90.2%), kWh (12.50 kWh), and SEK (125.50 SEK) formatting.
</objective>

<execution_context>
@/Users/julian.nordgren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julian.nordgren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-bug-fixes-polish/13-CONTEXT.md
@.planning/phases/13-bug-fixes-polish/13-RESEARCH.md
@src/lib/utils.ts
@src/actions/share.ts
@src/components/calculations/breakdowns/spotpris-breakdown.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create formatPercentage utility and fix efficiency calculation</name>
  <files>
    src/lib/utils.ts
    src/actions/share.ts
  </files>
  <action>
1. Add formatPercentage utility to src/lib/utils.ts:
   ```typescript
   /**
    * Format decimal as percentage with max precision, auto-trim trailing zeros
    * @param decimal - Value between 0 and 1 (e.g., 0.902 for 90.2%)
    * @param maxDecimals - Maximum decimal places (default: 2)
    * @returns Formatted string with % symbol (e.g., "90.2%")
    */
   export function formatPercentage(decimal: number, maxDecimals = 2): string {
     const percentage = decimal * 100
     return `${parseFloat(percentage.toFixed(maxDecimals))}%`
   }
   ```

2. Fix efficiency calculation in src/actions/share.ts buildPublicBreakdown inputs (around line 473):
   - CURRENT: `efficiency: Number(config.chargeEfficiency) * Number(config.dischargeEfficiency)`
   - FIX: `efficiency: (Number(config.chargeEfficiency) / 100) * (Number(config.dischargeEfficiency) / 100)`
   - The chargeEfficiency and dischargeEfficiency are stored as percentages (95.00 for 95%), must divide by 100 to get decimal (0.95)
  </action>
  <verify>
Run TypeScript compilation: `npx tsc --noEmit`
Verify formatPercentage exists: `grep -n "formatPercentage" src/lib/utils.ts`
Verify efficiency fix: `grep -n "efficiency.*100" src/actions/share.ts`
  </verify>
  <done>
formatPercentage utility exported from utils.ts, efficiency calculation divides by 100 before multiplication
  </done>
</task>

<task type="auto">
  <name>Task 2: Update spotpris-breakdown display formatting</name>
  <files>
    src/components/calculations/breakdowns/spotpris-breakdown.tsx
  </files>
  <action>
1. Import formatPercentage from utils:
   ```typescript
   import { formatPercentage } from '@/lib/utils'
   ```

2. Update Verkningsgrad display (around line 51):
   - CURRENT: `{(efficiency * 100).toFixed(0)}%`
   - FIX: `{formatPercentage(efficiency)}`
   - This will properly format 0.902 as "90.2%" with trailing zeros removed

3. Update formatSek function for currency formatting:
   - CURRENT: `const formatSek = (n: number) => Math.round(n).toLocaleString('sv-SE') + ' kr'`
   - FIX: Keep for rounded display, but add formatSekDecimal for 2-decimal precision:
   ```typescript
   const formatSekDecimal = (n: number) => n.toFixed(2) + ' SEK'
   ```

4. Update daily energy display (around line 59):
   - CURRENT: `{dailyKwh.toFixed(1)} kWh/dag`
   - FIX: `{dailyKwh.toFixed(2)} kWh` (2 decimals per CONTEXT.md decision, remove /dag for cleaner label)

5. Update daily savings display (around line 67):
   - CURRENT: `{dailySavings.toFixed(0)} kr/dag`
   - FIX: `{formatSekDecimal(dailySavings)}` using the new function (2 decimals, SEK suffix)

6. Ensure Swedish labels match CONTEXT.md decisions:
   - "Verkningsgrad" (correct)
   - "Daglig energi" (change from "= Daglig energi")
   - "Daglig besparing" (change from "= Daglig besparing")
  </action>
  <verify>
Run: `npx tsc --noEmit` (no type errors)
Verify import: `grep -n "formatPercentage" src/components/calculations/breakdowns/spotpris-breakdown.tsx`
Verify formatting: `grep -n "toFixed(2)" src/components/calculations/breakdowns/spotpris-breakdown.tsx`
  </verify>
  <done>
SpotprisBreakdown displays: Verkningsgrad as X.X% (no trailing zeros), Daglig energi as XX.XX kWh, Daglig besparing as XX.XX SEK
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. formatPercentage utility is exported and used
3. Efficiency calculation in share.ts divides by 100
4. Breakdown uses consistent 2-decimal formatting
</verification>

<success_criteria>
- FIX-01: Spotpris efficiency displays as percentage (e.g., 90.2% not 90000.2%)
- FIX-04: Breakdown shows Verkningsgrad (%), Daglig energi (kWh), Daglig besparing (SEK) with correct values
- All percentages use formatPercentage utility for consistency
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-bug-fixes-polish/13-01-SUMMARY.md`
</output>
