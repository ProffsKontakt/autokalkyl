---
phase: 10-consumption-profiles
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/calculations/consumption-profiles.ts
  - src/lib/calculations/consumption-profiles.test.ts
autonomous: true

must_haves:
  truths:
    - "distributeAnnualConsumption returns 12 monthly values that sum to annual input"
    - "estimateAnnualConsumption produces realistic estimates for Swedish households"
    - "Winter months show higher values for electric heating types"
    - "Fjarrvarme shows flat profile (only household electricity)"
  artifacts:
    - path: "src/lib/calculations/consumption-profiles.ts"
      provides: "HeatingType to monthly distribution mapping"
      exports: ["HEATING_TYPE_PROFILES", "distributeAnnualConsumption", "estimateAnnualConsumption"]
    - path: "src/lib/calculations/consumption-profiles.test.ts"
      provides: "Test coverage for consumption profile functions"
      min_lines: 50
  key_links:
    - from: "src/lib/calculations/consumption-profiles.ts"
      to: "@prisma/client HeatingType"
      via: "import type"
      pattern: "import.*HeatingType.*@prisma/client"
---

<objective>
Create consumption profile distribution logic with comprehensive test coverage.

Purpose: Phase 10 core logic that converts annual kWh + heating type into realistic monthly consumption distribution. This pure logic is the foundation for all UI components in subsequent plans.

Output: Tested consumption-profiles.ts with HEATING_TYPE_PROFILES mapping, distributeAnnualConsumption function, and estimateAnnualConsumption helper.
</objective>

<execution_context>
@/Users/julian.nordgren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julian.nordgren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-consumption-profiles/10-CONTEXT.md
@.planning/phases/10-consumption-profiles/10-RESEARCH.md
@prisma/schema.prisma (HeatingType enum lines 79-85)
@src/lib/calculations/presets.ts (existing monthly factors pattern)
@src/lib/calculations/constants.ts (MONTH_NAMES_SV)
</context>

<feature>
  <name>Consumption Profile Distribution</name>
  <files>src/lib/calculations/consumption-profiles.ts, src/lib/calculations/consumption-profiles.test.ts</files>
  <behavior>
    distributeAnnualConsumption(annualKwh: number, heatingType: HeatingType) -> number[]
    - Input: 20000, DIREKTVERKANDE -> [~2500, ~2333, ~2000, ~1500, ~917, ~667, ~583, ~667, ~1000, ~1583, ~2083, ~2500]
    - Input: 20000, FJARRVARME -> [~1750, ~1717, ~1667, ~1633, ~1583, ~1533, ~1500, ~1533, ~1633, ~1700, ~1750, ~1800]
    - Sum of output MUST equal annualKwh (within rounding tolerance)
    - Winter months (Dec, Jan, Feb) > Summer months (Jun, Jul, Aug) for DIREKTVERKANDE
    - Factors must be normalized (sum to 12)

    estimateAnnualConsumption(input: { houseSizeM2, residents, heatingType }) -> number
    - Input: { houseSizeM2: 150, residents: 4, heatingType: DIREKTVERKANDE } -> 26000
    - Input: { houseSizeM2: 150, residents: 4, heatingType: BERGVARME } -> 15500
    - Input: { houseSizeM2: 100, residents: 2, heatingType: FJARRVARME } -> 4000 (household only)
    - Result rounded to nearest 500 kWh
  </behavior>
  <implementation>
    1. Create HEATING_TYPE_PROFILES constant mapping HeatingType enum to:
       - name (Swedish display name)
       - description (Swedish tooltip text)
       - monthlyFactors (12 values summing to 12, representing relative consumption)

    2. Implement distributeAnnualConsumption:
       - Look up profile for heatingType
       - Calculate monthlyAvg = annualKwh / 12
       - Return monthlyFactors.map(factor => monthlyAvg * factor)

    3. Implement estimateAnnualConsumption:
       - HEATING_KWH_PER_M2 lookup (DIREKTVERKANDE: 120, BERGVARME: 50, etc.)
       - HOUSEHOLD_KWH_PER_PERSON = 2000
       - heatingKwh = houseSizeM2 * kwhPerM2
       - householdKwh = residents * 2000
       - Round to nearest 500

    Monthly factors from RESEARCH.md:
    - BERGVARME: [1.25, 1.20, 1.10, 0.90, 0.75, 0.65, 0.60, 0.65, 0.80, 1.00, 1.15, 1.25]
    - FJARRVARME: [1.05, 1.03, 1.00, 0.98, 0.95, 0.92, 0.90, 0.92, 0.98, 1.02, 1.05, 1.08]
    - DIREKTVERKANDE: [1.50, 1.40, 1.20, 0.90, 0.55, 0.40, 0.35, 0.40, 0.60, 0.95, 1.25, 1.50]
    - LUFT_LUFT_VP: [1.35, 1.28, 1.12, 0.88, 0.68, 0.55, 0.50, 0.55, 0.72, 0.95, 1.18, 1.35]
    - LUFT_VATTEN_VP: [1.30, 1.22, 1.08, 0.88, 0.70, 0.58, 0.55, 0.60, 0.75, 0.98, 1.18, 1.30]
  </implementation>
</feature>

<verification>
- `npm test src/lib/calculations/consumption-profiles.test.ts` passes all tests
- TypeScript compilation succeeds with no errors
- Each HeatingType has a profile defined
- Monthly factors sum to 12 for each profile
</verification>

<success_criteria>
- Tests cover all 5 heating types for both functions
- distributeAnnualConsumption output sums to input annual kWh
- Winter > summer for electric heating types (DIREKTVERKANDE, LUFT_LUFT_VP, LUFT_VATTEN_VP)
- estimateAnnualConsumption returns values in realistic range (5000-75000)
</success_criteria>

<output>
After completion, create `.planning/phases/10-consumption-profiles/10-01-SUMMARY.md`
</output>
