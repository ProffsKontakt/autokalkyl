---
phase: 10-consumption-profiles
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/calculations/wizard/steps/consumption-profile-step.tsx
  - src/components/calculations/wizard/calculation-wizard.tsx
  - src/components/calculations/results/consumption-distribution-section.tsx
autonomous: false

must_haves:
  truths:
    - "Wizard has consumption profile as a required step"
    - "Cannot proceed from consumption step without annual kWh and heating type"
    - "Distribution chart shows in wizard preview"
    - "Distribution chart shows on results page"
    - "Winter months show higher consumption for electric heating types"
  artifacts:
    - path: "src/components/calculations/wizard/steps/consumption-profile-step.tsx"
      provides: "New wizard step assembling consumption profile components"
      min_lines: 60
    - path: "src/components/calculations/wizard/calculation-wizard.tsx"
      provides: "Updated wizard with consumption profile step"
      contains: "ConsumptionProfileStep"
    - path: "src/components/calculations/results/consumption-distribution-section.tsx"
      provides: "Results page distribution chart section"
      min_lines: 40
  key_links:
    - from: "src/components/calculations/wizard/calculation-wizard.tsx"
      to: "src/components/calculations/wizard/steps/consumption-profile-step.tsx"
      via: "import and render"
      pattern: "ConsumptionProfileStep"
    - from: "src/components/calculations/wizard/steps/consumption-profile-step.tsx"
      to: "src/stores/calculation-wizard-store.ts"
      via: "useCalculationWizardStore"
      pattern: "useCalculationWizardStore"
---

<objective>
Integrate consumption profile into wizard flow and results page.

Purpose: Connect all the pieces - assemble UI components into a wizard step, update wizard navigation/validation, and add distribution visualization to results page.

Output: Working consumption profile wizard step, updated wizard flow, distribution chart on results page.
</objective>

<execution_context>
@/Users/julian.nordgren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julian.nordgren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-consumption-profiles/10-CONTEXT.md
@.planning/phases/10-consumption-profiles/10-RESEARCH.md
@.planning/phases/10-consumption-profiles/10-01-SUMMARY.md
@src/components/calculations/wizard/calculation-wizard.tsx
@src/components/calculations/wizard/steps/customer-info-step.tsx (step pattern)
@src/components/calculations/wizard/steps/results-step.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create consumption profile wizard step</name>
  <files>src/components/calculations/wizard/steps/consumption-profile-step.tsx</files>
  <action>
    Create the wizard step that assembles all consumption profile components:

    ```tsx
    'use client'

    import { useCalculationWizardStore } from '@/stores/calculation-wizard-store'
    import { AnnualKwhInput } from '../consumption-profile/annual-kwh-input'
    import { HeatingTypeSelect } from '../consumption-profile/heating-type-select'
    import { EstimationHelper } from '../consumption-profile/estimation-helper'
    import { DistributionChart } from '../consumption-profile/distribution-chart'
    ```

    Layout structure:
    1. Title: "Forbrukningsprofil"
    2. Two-column layout on large screens, single column on mobile
    3. Left column:
       - HeatingTypeSelect (required, no default)
       - AnnualKwhInput (range 5000-75000, step 500)
       - EstimationHelper (collapsible)
    4. Right column:
       - DistributionChart (live preview)

    Connect to store:
    - Read annualConsumptionKwh, heatingType from store
    - Call updateCustomerInfo({ annualConsumptionKwh }) on slider change
    - Call updateHeatingType() on radio change
    - Pass values to DistributionChart for live preview

    Summary card at bottom (like customer-info-step.tsx):
    - Show selected heating type name
    - Show annual consumption formatted
    - Only show when both fields are filled
  </action>
  <verify>
    Component renders without errors.
    Changing slider updates chart immediately.
    Changing heating type updates chart immediately.
  </verify>
  <done>
    ConsumptionProfileStep displays all inputs and live chart preview.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate step into wizard and update validation</name>
  <files>src/components/calculations/wizard/calculation-wizard.tsx</files>
  <action>
    Update the calculation wizard to include consumption profile step:

    1. Import ConsumptionProfileStep:
       ```tsx
       import { ConsumptionProfileStep } from './steps/consumption-profile-step'
       ```

    2. Update TOTAL_STEPS from 4 to 5

    3. Update step rendering order:
       - Step 0: CustomerInfoStep (unchanged)
       - Step 1: ConsumptionProfileStep (NEW)
       - Step 2: ConsumptionStep (was Step 1 - the 12x24 editor)
       - Step 3: BatteryStep (was Step 2)
       - Step 4: ResultsStep (was Step 3)

    4. Update canGoNext() validation:
       - Case 0 (Customer Info): Remove annualConsumptionKwh check (moved to step 1)
       - Case 1 (Consumption Profile):
         ```tsx
         return !!(
           store.annualConsumptionKwh >= 5000 &&
           store.annualConsumptionKwh <= 75000 &&
           store.heatingType !== null
         )
         ```
       - Case 2, 3, 4: shift existing cases

    5. Adjust CustomerInfoStep usage:
       - Remove annualConsumptionKwh input if it exists there (it's now in step 1)
       - Keep customerName, postalCode, elomrade, natagareId

    NOTE: Check customer-info-step.tsx first. If annualConsumptionKwh is there, it needs to remain for backward compatibility OR be removed with care. The step should focus on customer identity, not consumption.
  </action>
  <verify>
    Wizard shows 5 steps total.
    Cannot proceed past step 1 without valid consumption profile.
    Step order is correct: Customer -> Consumption Profile -> 12x24 Editor -> Battery -> Results.
  </verify>
  <done>
    Consumption profile is step 2 (index 1) and blocks progression when incomplete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add distribution chart to results page</name>
  <files>src/components/calculations/results/consumption-distribution-section.tsx</files>
  <action>
    Create a results section component showing the consumption distribution:

    1. Create new component:
       ```tsx
       'use client'

       import { DistributionChart } from '../wizard/consumption-profile/distribution-chart'
       import { HEATING_TYPE_PROFILES } from '@/lib/calculations/consumption-profiles'
       import type { HeatingType } from '@prisma/client'

       interface ConsumptionDistributionSectionProps {
         annualKwh: number
         heatingType: HeatingType | null
       }
       ```

    2. Layout:
       - Section wrapper with heading "Forbrukningsprofil"
       - Reuse DistributionChart component
       - If heatingType is null, show fallback message: "Ingen uppvarmningstyp vald"
       - Below chart: Show heating type name and description

    3. Make this a collapsible/expandable section to match other results sections
       - Default expanded
       - ChevronDown/ChevronUp toggle

    4. This component will be imported into results-step.tsx or the public results page.
       For now, create the component - integration with results page will happen when
       results-step.tsx is updated to show the chart (may need minor update to pass heatingType).

    NOTE: The results-step.tsx may need to be updated to pass heatingType from store.
    Check if it already has access to heatingType or if it needs to be added.
  </action>
  <verify>
    Component renders with sample props.
    Chart displays correctly with seasonal pattern.
    Collapsible behavior works.
  </verify>
  <done>
    ConsumptionDistributionSection ready for integration with results page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete consumption profile wizard step with:
    - Slider + text input for annual kWh (5000-75000, step 500)
    - Radio buttons for 5 Swedish heating types
    - Estimation helper (house size + residents)
    - Live distribution chart preview
    - Results page distribution section
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to /dashboard/calculations/new (login as Closer or Org Admin)
    3. Complete Customer Info step (name, elomrade, natagare)
    4. Verify new Consumption Profile step appears as Step 2:
       - Select a heating type (e.g., Direktverkande el)
       - Observe chart shows high winter, low summer pattern
       - Adjust slider and verify chart updates immediately
       - Try typing exact value in text input (e.g., 18500)
       - Expand estimation helper, enter 150 m2 + 4 residents, click Berakna
       - Verify estimated value populates slider
    5. Try to proceed without selecting heating type - should be blocked
    6. Complete remaining steps (12x24 editor, battery selection)
    7. On Results step, verify consumption distribution chart appears
    8. Check seasonal pattern:
       - DIREKTVERKANDE: highest in Dec/Jan, lowest in Jun/Jul
       - FJARRVARME: nearly flat line
       - BERGVARME: moderate seasonal variation
  </how-to-verify>
  <resume-signal>Type "approved" if consumption profile step works correctly, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run dev` runs without errors
- Wizard flow includes consumption profile step
- Validation prevents proceeding without heating type selection
- Chart displays on both wizard preview and results
</verification>

<success_criteria>
- CONS-01: Annual kWh input via slider + text (5000-75000 range)
- CONS-02: 5 Swedish heating types selectable via radio buttons
- CONS-03: Monthly distribution generated from heating type selection
- CONS-04: Visual chart in wizard preview AND results page
- CONS-05: Winter months higher for electric heating types, flat for fjarrvarme
</success_criteria>

<output>
After completion, create `.planning/phases/10-consumption-profiles/10-03-SUMMARY.md`
</output>
