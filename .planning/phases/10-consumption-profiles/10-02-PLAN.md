---
phase: 10-consumption-profiles
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/stores/calculation-wizard-store.ts
  - src/components/calculations/wizard/consumption-profile/annual-kwh-input.tsx
  - src/components/calculations/wizard/consumption-profile/heating-type-select.tsx
  - src/components/calculations/wizard/consumption-profile/estimation-helper.tsx
  - src/components/calculations/wizard/consumption-profile/distribution-chart.tsx
autonomous: true

must_haves:
  truths:
    - "Wizard store persists heatingType across browser refresh"
    - "Slider and text input stay synchronized for annual kWh"
    - "Radio buttons display all 5 Swedish heating types with descriptions"
    - "Chart updates live when annual kWh or heating type changes"
  artifacts:
    - path: "src/stores/calculation-wizard-store.ts"
      provides: "heatingType state and persistence"
      contains: "heatingType"
    - path: "src/components/calculations/wizard/consumption-profile/annual-kwh-input.tsx"
      provides: "Slider with text override component"
      min_lines: 40
    - path: "src/components/calculations/wizard/consumption-profile/heating-type-select.tsx"
      provides: "Radio button group for heating types"
      min_lines: 40
    - path: "src/components/calculations/wizard/consumption-profile/estimation-helper.tsx"
      provides: "House size estimation form"
      min_lines: 50
    - path: "src/components/calculations/wizard/consumption-profile/distribution-chart.tsx"
      provides: "Recharts AreaChart for monthly distribution"
      min_lines: 50
  key_links:
    - from: "src/stores/calculation-wizard-store.ts"
      to: "@prisma/client HeatingType"
      via: "import type"
      pattern: "heatingType.*HeatingType"
    - from: "src/components/calculations/wizard/consumption-profile/distribution-chart.tsx"
      to: "src/lib/calculations/consumption-profiles.ts"
      via: "import distributeAnnualConsumption"
      pattern: "distributeAnnualConsumption"
---

<objective>
Create UI components for consumption profile input and extend wizard store.

Purpose: Build the user interface components that Closers use to input annual consumption and heating type. These components will be assembled into a wizard step in Plan 03.

Output: Extended wizard store with heatingType, plus 4 new UI components (slider, radio buttons, estimation helper, chart).
</objective>

<execution_context>
@/Users/julian.nordgren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julian.nordgren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-consumption-profiles/10-CONTEXT.md
@.planning/phases/10-consumption-profiles/10-RESEARCH.md
@.planning/phases/10-consumption-profiles/10-01-SUMMARY.md
@src/stores/calculation-wizard-store.ts
@src/components/calculations/controls/cycles-slider.tsx (slider pattern)
@src/components/calculations/results/roi-timeline-chart.tsx (Recharts pattern)
@src/lib/calculations/consumption-profiles.ts (from Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend wizard store with heatingType</name>
  <files>src/stores/calculation-wizard-store.ts</files>
  <action>
    Add heatingType to the calculation wizard store:

    1. Add import: `import type { HeatingType } from '@prisma/client'`

    2. Add to WizardState interface:
       ```typescript
       heatingType: HeatingType | null
       ```

    3. Add action to interface:
       ```typescript
       updateHeatingType: (type: HeatingType | null) => void
       ```

    4. Add to initialState:
       ```typescript
       heatingType: null as HeatingType | null,
       ```

    5. Add action implementation in create():
       ```typescript
       updateHeatingType: (type) => set({ heatingType: type }),
       ```

    6. Add to partialize (for localStorage persistence):
       ```typescript
       heatingType: state.heatingType,
       ```

    7. Add to loadFromServer data interface and implementation:
       ```typescript
       heatingType?: HeatingType | null
       ```
       And in the set(): `heatingType: data.heatingType ?? null,`

    8. Add to reset():
       ```typescript
       heatingType: null,
       ```

    DO NOT change the localStorage key version - heatingType is a new optional field that won't break existing drafts.
  </action>
  <verify>
    TypeScript compilation passes with `npx tsc --noEmit`.
    Store exports heatingType and updateHeatingType.
  </verify>
  <done>
    heatingType persisted in localStorage when set; survives browser refresh.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create annual kWh input and heating type select components</name>
  <files>
    src/components/calculations/wizard/consumption-profile/annual-kwh-input.tsx
    src/components/calculations/wizard/consumption-profile/heating-type-select.tsx
  </files>
  <action>
    Create two input components following existing patterns:

    **annual-kwh-input.tsx:**
    - Props: value: number, onChange: (value: number) => void
    - Slider range: 5000-75000 kWh, step 500
    - Text input synced with slider (both ways)
    - Clamp text input to valid range on blur
    - Use framer-motion for value animation (like cycles-slider.tsx)
    - Swedish labels: "Arlig forbrukning", "kWh/ar"
    - Show min/mid/max labels under slider

    Pattern from cycles-slider.tsx:
    ```tsx
    <motion.span
      key={value}
      initial={{ scale: 1.3, color: '#3B82F6' }}
      animate={{ scale: 1, color: '#1F2937' }}
    >
      {value.toLocaleString('sv-SE')}
    </motion.span>
    ```

    **heating-type-select.tsx:**
    - Props: value: HeatingType | null, onChange: (type: HeatingType) => void
    - Import HeatingType from @prisma/client
    - Import HEATING_TYPE_PROFILES from consumption-profiles.ts
    - Render as fieldset with radio buttons
    - Each option shows name + description from HEATING_TYPE_PROFILES
    - Visual feedback for selected option (border-blue-500, bg-blue-50)
    - Required field indicator (*)

    Use Tailwind classes matching existing form styling in customer-info-step.tsx.
  </action>
  <verify>
    Both components render without errors.
    Slider and input stay synced when either changes.
    Radio selection highlights correctly.
  </verify>
  <done>
    AnnualKwhInput accepts value/onChange, renders slider + input.
    HeatingTypeSelect displays all 5 types with Swedish names and descriptions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create estimation helper and distribution chart components</name>
  <files>
    src/components/calculations/wizard/consumption-profile/estimation-helper.tsx
    src/components/calculations/wizard/consumption-profile/distribution-chart.tsx
  </files>
  <action>
    Create two supporting components:

    **estimation-helper.tsx:**
    - Props: heatingType: HeatingType | null, onEstimate: (kWh: number) => void
    - Inline expandable section (not modal per RESEARCH.md recommendation)
    - Collapsible with ChevronDown/ChevronUp icon
    - Inputs: houseSizeM2 (number), residents (number)
    - Uses estimateAnnualConsumption from consumption-profiles.ts
    - Button "Berakna" calculates and calls onEstimate with result
    - Disabled state if heatingType is null (show hint to select heating type first)
    - Swedish labels throughout

    Collapsible pattern:
    ```tsx
    const [isOpen, setIsOpen] = useState(false)
    // Toggle button with ChevronDown/ChevronUp
    // AnimatePresence + motion.div for smooth expand/collapse
    ```

    **distribution-chart.tsx:**
    - Props: annualKwh: number, heatingType: HeatingType | null
    - Uses distributeAnnualConsumption from consumption-profiles.ts
    - Recharts AreaChart with ResponsiveContainer
    - X-axis: months (Jan-Dec, use MONTH_NAMES_SV shortened to 3 chars)
    - Y-axis: kWh with formatter showing "Xk"
    - Gradient fill (blue gradient like ROI chart)
    - Tooltip showing exact kWh value
    - Shows total annual kWh in header
    - Graceful handling when heatingType is null (show placeholder message)

    Follow existing Recharts pattern from roi-timeline-chart.tsx.
  </action>
  <verify>
    EstimationHelper expands/collapses smoothly.
    DistributionChart renders with sample data.
    Chart updates when annualKwh or heatingType changes.
  </verify>
  <done>
    EstimationHelper calculates realistic estimates using formula.
    DistributionChart shows monthly distribution with correct seasonal pattern.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 4 new components exist and export correctly
- Store heatingType persists to localStorage
</verification>

<success_criteria>
- heatingType in store survives browser refresh
- AnnualKwhInput slider and text input stay synchronized
- HeatingTypeSelect shows all 5 types with descriptions
- EstimationHelper calculates from house size + residents
- DistributionChart displays monthly bars with seasonal pattern
</success_criteria>

<output>
After completion, create `.planning/phases/10-consumption-profiles/10-02-SUMMARY.md`
</output>
