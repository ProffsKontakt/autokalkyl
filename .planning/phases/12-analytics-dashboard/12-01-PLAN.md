---
phase: 12-analytics-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/analytics/posthog-server.ts
  - src/lib/analytics/server-events.ts
  - src/components/analytics/posthog-provider.tsx
autonomous: true
user_setup:
  - service: posthog
    why: "Server-side event capture requires API key (not public key)"
    env_vars:
      - name: POSTHOG_API_KEY
        source: "PostHog Dashboard -> Project Settings -> API keys -> Project API Key (not public key)"
      - name: POSTHOG_PROJECT_ID
        source: "PostHog Dashboard -> Project Settings -> Project ID (numeric)"
      - name: POSTHOG_PERSONAL_API_KEY
        source: "PostHog Dashboard -> Settings -> Personal API Keys -> Create key with 'query:read' scope"

must_haves:
  truths:
    - "PostHog events are not blocked by bot detection"
    - "Server-side events can be captured from server actions"
    - "Client-side PostHog still works for UI interactions"
  artifacts:
    - path: "src/lib/analytics/posthog-server.ts"
      provides: "Server-side PostHog singleton with flushAt:1 and shutdown()"
      exports: ["getServerPostHog", "captureServerEvent"]
    - path: "src/lib/analytics/server-events.ts"
      provides: "Type-safe server event capture functions"
      exports: ["trackCalculationCreated", "trackCalculationUpdated", "trackCalculationDeleted", "trackCalculationViewed", "trackShareLinkGenerated"]
    - path: "src/components/analytics/posthog-provider.tsx"
      provides: "Client-side PostHog config with bot detection fix"
      contains: "opt_out_useragent_filter: true"
  key_links:
    - from: "src/lib/analytics/posthog-server.ts"
      to: "posthog-node"
      via: "import"
      pattern: "from 'posthog-node'"
    - from: "src/lib/analytics/server-events.ts"
      to: "src/lib/analytics/posthog-server.ts"
      via: "import getServerPostHog"
      pattern: "from './posthog-server'"
---

<objective>
Set up PostHog server-side event capture infrastructure and fix client-side bot detection.

Purpose: Reliable analytics requires server-side event capture (ad blockers block 30% of client-side events). This plan establishes the foundation for all business-critical event tracking.

Output: Server-side PostHog singleton, event capture functions, and fixed client-side config.
</objective>

<execution_context>
@/Users/julian.nordgren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julian.nordgren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-analytics-dashboard/12-RESEARCH.md

@src/components/analytics/posthog-provider.tsx
@src/lib/analytics/posthog.ts
@src/lib/analytics/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install posthog-node and create server-side singleton</name>
  <files>
    package.json
    src/lib/analytics/posthog-server.ts
  </files>
  <action>
1. Install posthog-node:
   ```bash
   npm install posthog-node
   ```

2. Create `src/lib/analytics/posthog-server.ts`:
   - Export `getServerPostHog()` function that returns a singleton PostHog client
   - Configure with `flushAt: 1` and `flushInterval: 0` (critical for serverless)
   - Use `process.env.POSTHOG_API_KEY` (server-side key, not NEXT_PUBLIC_)
   - Use `process.env.POSTHOG_HOST` or default to 'https://eu.i.posthog.com'

3. Export `captureServerEvent(distinctId, event, properties)` helper that:
   - Gets the singleton client
   - Calls `client.capture({ distinctId, event, properties })`
   - Calls `await client.shutdown()` to flush (critical for serverless)

Reference the pattern in 12-RESEARCH.md "Pattern 1: Server-Side PostHog Client Singleton".
  </action>
  <verify>
    - `npm ls posthog-node` shows package installed
    - TypeScript compiles: `npx tsc --noEmit src/lib/analytics/posthog-server.ts`
  </verify>
  <done>
    posthog-node installed, server-side singleton exports getServerPostHog and captureServerEvent with proper serverless flush settings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create server-side event capture functions</name>
  <files>
    src/lib/analytics/server-events.ts
  </files>
  <action>
Create `src/lib/analytics/server-events.ts` with type-safe event capture functions:

1. `trackCalculationCreated(userId, calculationId, orgId, customerName)`:
   - Event: 'calculation_created'
   - Properties: calculation_id, org_id, closer_id, customer_name, $set: { last_calculation_at }

2. `trackCalculationUpdated(userId, calculationId, orgId, status)`:
   - Event: 'calculation_updated'
   - Properties: calculation_id, org_id, closer_id, status

3. `trackCalculationDeleted(userId, calculationId, orgId)`:
   - Event: 'calculation_deleted'
   - Properties: calculation_id, org_id, closer_id

4. `trackCalculationViewed(calculationId, orgId, closerId, viewerType: 'prospect' | 'closer' | 'admin')`:
   - Event: 'calculation_viewed'
   - For prospects: use `calc_${calculationId}` as distinctId (anonymous)
   - For authenticated users: use closerId
   - Properties: calculation_id, org_id, closer_id, viewer_type

5. `trackShareLinkGenerated(userId, calculationId, orgId, hasPassword, hasExpiration)`:
   - Event: 'share_link_generated'
   - Properties: calculation_id, org_id, closer_id, has_password, has_expiration

6. `trackWizardCompleted(userId, calculationId, orgId, batteryCount)`:
   - Event: 'wizard_completed'
   - Properties: calculation_id, org_id, closer_id, battery_count

All functions should:
- Import `captureServerEvent` from './posthog-server'
- Be async and return Promise<void>
- Be exported individually for tree-shaking

Reference 12-RESEARCH.md "Pattern 2: Server-Side Event Capture in Actions".
  </action>
  <verify>
    - TypeScript compiles: `npx tsc --noEmit src/lib/analytics/server-events.ts`
    - All 6 functions exported
  </verify>
  <done>
    Type-safe server event functions exist covering the full calculation lifecycle (create, update, delete, view, share, complete).
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix client-side bot detection</name>
  <files>
    src/components/analytics/posthog-provider.tsx
  </files>
  <action>
Modify `src/components/analytics/posthog-provider.tsx`:

1. Add `opt_out_useragent_filter: true` to the posthog.init() options:
   ```typescript
   posthog.init(key, {
     api_host: host,
     capture_pageview: false,
     capture_pageleave: true,
     person_profiles: 'identified_only',
     autocapture: true,
     // FIX: Disable bot detection that blocks real traffic
     opt_out_useragent_filter: true,
     // ... rest of existing config
   })
   ```

This single line fix addresses ANLY-07 (bot detection blocking events).

Reference 12-RESEARCH.md "Bot Detection Fix" and "Pitfall 2".
  </action>
  <verify>
    - File contains `opt_out_useragent_filter: true`
    - TypeScript compiles without errors
  </verify>
  <done>
    Client-side PostHog will no longer block events based on user agent filtering.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npm run build` succeeds (no TypeScript errors)
2. `grep -r "opt_out_useragent_filter" src/` shows the fix applied
3. `grep -r "posthog-node" package.json` shows dependency installed
4. Server-side module exports expected functions: `cat src/lib/analytics/posthog-server.ts | grep "export"`
</verification>

<success_criteria>
- ANLY-07 PARTIAL: Bot detection disabled (full requirement needs event flow verification in Plan 03)
- posthog-node installed and configured for serverless
- Server-side event capture functions ready for integration
- No breaking changes to existing client-side analytics
</success_criteria>

<output>
After completion, create `.planning/phases/12-analytics-dashboard/12-01-SUMMARY.md`
</output>
