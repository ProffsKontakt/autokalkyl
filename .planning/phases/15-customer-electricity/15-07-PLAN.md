---
phase: 15-customer-electricity
plan: 07
type: execute
wave: 4
depends_on: ["15-06"]
files_modified:
  - src/components/calculations/public/calculation-summary.tsx
  - src/components/calculations/calculation-detail.tsx
autonomous: false

must_haves:
  truths:
    - "Customer type is visible in calculation summary (expanded details)"
    - "Customer type is visible in public prospect view (expanded details)"
    - "Foretag label displays VAT exclusion note"
    - "Electricity data shown in appropriate detail sections"
  artifacts:
    - path: "src/components/calculations/public/calculation-summary.tsx"
      provides: "Public view showing customer type and electricity data"
      contains: "customerType"
    - path: "src/components/calculations/calculation-detail.tsx"
      provides: "Internal detail view showing customer type"
      contains: "customerType"
  key_links:
    - from: "src/components/calculations/public/calculation-summary.tsx"
      to: "calculation data"
      via: "props or query"
      pattern: "customerType|koptElKwh"
---

<objective>
Display customer type and electricity data in calculation summary and public view.

Purpose: CUST-04 requires customer type visible in summary and public view. Closers and prospects need to see the data entered.
Output: Updated summary components showing customer type in expanded details section.
</objective>

<execution_context>
@/Users/julian.nordgren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julian.nordgren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-customer-electricity/15-CONTEXT.md
@.planning/phases/15-customer-electricity/15-06-SUMMARY.md
@src/components/calculations/public/calculation-summary.tsx
@src/components/calculations/calculation-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add customer type and electricity data to public calculation summary</name>
  <files>src/components/calculations/public/calculation-summary.tsx</files>
  <action>
Update src/components/calculations/public/calculation-summary.tsx to display Phase 15 data.

1. Update the component props interface to include Phase 15 fields:
```typescript
interface CalculationSummaryProps {
  // ... existing props
  customerType?: string
  koptElKwh?: number
  electricityPriceOreKwh?: number
  hasSolar?: boolean
  solarProductionKwh?: number
  currentSelfConsumptionKwh?: number
  projectedSelfConsumptionKwh?: number
}
```

2. In the expanded details section (likely an accordion or expandable area), add a "Kundinformation" or "Elinformation" section:

```tsx
{/* Customer & Electricity Details - in expanded section */}
<div className="border-t border-gray-200 dark:border-gray-700 pt-4 mt-4">
  <h4 className="text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
    Elinformation
  </h4>
  <dl className="text-sm space-y-1">
    {customerType && (
      <div className="flex justify-between">
        <dt className="text-gray-500 dark:text-gray-400">Kundtyp:</dt>
        <dd className="font-medium text-gray-900 dark:text-white">
          {customerType === 'FORETAG' ? 'Foretag (exkl. moms)' : 'Privatperson'}
        </dd>
      </div>
    )}
    {koptElKwh && koptElKwh > 0 && (
      <div className="flex justify-between">
        <dt className="text-gray-500 dark:text-gray-400">Kopt el:</dt>
        <dd className="font-medium text-gray-900 dark:text-white">
          {koptElKwh.toLocaleString('sv-SE')} kWh/ar
        </dd>
      </div>
    )}
    {electricityPriceOreKwh && electricityPriceOreKwh > 0 && (
      <div className="flex justify-between">
        <dt className="text-gray-500 dark:text-gray-400">Elpris:</dt>
        <dd className="font-medium text-gray-900 dark:text-white">
          {electricityPriceOreKwh.toFixed(0)} ore/kWh
        </dd>
      </div>
    )}
    {hasSolar && solarProductionKwh && solarProductionKwh > 0 && (
      <>
        <div className="flex justify-between">
          <dt className="text-gray-500 dark:text-gray-400">Solproduktion:</dt>
          <dd className="font-medium text-gray-900 dark:text-white">
            {solarProductionKwh.toLocaleString('sv-SE')} kWh/ar
          </dd>
        </div>
        {currentSelfConsumptionKwh !== undefined && currentSelfConsumptionKwh !== null && solarProductionKwh > 0 && (
          <div className="flex justify-between">
            <dt className="text-gray-500 dark:text-gray-400">Egenanvandning idag:</dt>
            <dd className="font-medium text-gray-900 dark:text-white">
              {((currentSelfConsumptionKwh / solarProductionKwh) * 100).toFixed(0)}%
            </dd>
          </div>
        )}
        {projectedSelfConsumptionKwh !== undefined && projectedSelfConsumptionKwh !== null && solarProductionKwh > 0 && (
          <div className="flex justify-between">
            <dt className="text-gray-500 dark:text-gray-400">Egenanvandning med batteri:</dt>
            <dd className="font-medium text-gray-900 dark:text-white">
              {((projectedSelfConsumptionKwh / solarProductionKwh) * 100).toFixed(0)}%
            </dd>
          </div>
        )}
      </>
    )}
  </dl>
</div>
```

Place this section in the expanded/details area so it's not prominently displayed on main summary (per CONTEXT.md: "Visible in expanded details only, not prominently displayed in main summary").
  </action>
  <verify>`npx tsc --noEmit` passes</verify>
  <done>Public calculation summary shows customer type and electricity data in expanded details section</done>
</task>

<task type="auto">
  <name>Task 2: Add customer type to internal calculation detail view</name>
  <files>src/components/calculations/calculation-detail.tsx</files>
  <action>
Update src/components/calculations/calculation-detail.tsx to display Phase 15 data for closers/admins.

1. If the component receives calculation data as props, ensure Phase 15 fields are typed:
```typescript
interface CalculationDetailProps {
  calculation: {
    // ... existing fields
    customerType?: string
    koptElKwh?: number
    electricityPriceOreKwh?: number
    hasSolar?: boolean
    solarProductionKwh?: number
    currentSelfConsumptionKwh?: number
    projectedSelfConsumptionKwh?: number
  }
}
```

2. Add an "Elinformation" section to the detail view (similar to public view but can be more prominent since this is the internal view):

```tsx
{/* Electricity Information Section */}
<div className="bg-white dark:bg-gray-800 rounded-lg shadow p-4">
  <h3 className="text-lg font-medium text-gray-900 dark:text-white mb-4">
    Elinformation
  </h3>
  <dl className="grid grid-cols-2 gap-4 text-sm">
    <div>
      <dt className="text-gray-500 dark:text-gray-400">Kundtyp</dt>
      <dd className="font-medium text-gray-900 dark:text-white">
        {calculation.customerType === 'FORETAG' ? 'Foretag (exkl. moms)' : 'Privatperson'}
      </dd>
    </div>
    {calculation.koptElKwh && (
      <div>
        <dt className="text-gray-500 dark:text-gray-400">Kopt el</dt>
        <dd className="font-medium text-gray-900 dark:text-white">
          {calculation.koptElKwh.toLocaleString('sv-SE')} kWh/ar
        </dd>
      </div>
    )}
    {calculation.electricityPriceOreKwh && (
      <div>
        <dt className="text-gray-500 dark:text-gray-400">Elpris</dt>
        <dd className="font-medium text-gray-900 dark:text-white">
          {calculation.electricityPriceOreKwh.toFixed(0)} ore/kWh
        </dd>
      </div>
    )}
    {calculation.hasSolar && calculation.solarProductionKwh && (
      <>
        <div>
          <dt className="text-gray-500 dark:text-gray-400">Solproduktion</dt>
          <dd className="font-medium text-gray-900 dark:text-white">
            {calculation.solarProductionKwh.toLocaleString('sv-SE')} kWh/ar
          </dd>
        </div>
        <div>
          <dt className="text-gray-500 dark:text-gray-400">Egenanv. idag / med batteri</dt>
          <dd className="font-medium text-gray-900 dark:text-white">
            {calculation.currentSelfConsumptionKwh && calculation.solarProductionKwh > 0
              ? `${((calculation.currentSelfConsumptionKwh / calculation.solarProductionKwh) * 100).toFixed(0)}%`
              : '-'}
            {' / '}
            {calculation.projectedSelfConsumptionKwh && calculation.solarProductionKwh > 0
              ? `${((calculation.projectedSelfConsumptionKwh / calculation.solarProductionKwh) * 100).toFixed(0)}%`
              : '-'}
          </dd>
        </div>
      </>
    )}
  </dl>
</div>
```

This should be placed in an appropriate location within the detail layout - likely near customer info or at the top of the details section.
  </action>
  <verify>`npx tsc --noEmit` passes</verify>
  <done>Internal calculation detail view shows customer type and electricity data</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 15 electricity input flow:
1. New ElectricityStep in wizard (step 2 of 6)
2. Customer type dropdown (Privatperson/Foretag)
3. Kopt el input with annual/monthly toggle
4. Electricity price input with ore/SEK toggle
5. Solar section with three-field self-consumption model
6. Auto-save persists all fields
7. Customer type visible in summary views
  </what-built>
  <how-to-verify>
1. Start development server: `npm run dev`
2. Navigate to dashboard and start new calculation
3. Complete step 1 (Customer Info) and click Next
4. Verify step 2 shows "Elforbrukning & Elpris" heading
5. Test customer type dropdown - select Foretag, verify text says "exkl. moms"
6. Enter kopt el value (e.g., 20000), toggle to monthly and verify values distribute
7. Enter electricity price (e.g., 150), toggle to kr/kWh and verify conversion (1.50)
8. Toggle "Har solceller?" on
9. Enter solar production (e.g., 10000), verify auto-suggested self-consumption values
10. Click Next - should proceed if all required fields filled
11. Complete remaining wizard steps and finalize
12. View calculation detail - verify customer type and electricity data shown
13. If share link available, view public page and verify data in expanded details
  </how-to-verify>
  <resume-signal>Type "approved" if flow works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript: `npx tsc --noEmit` passes
2. Customer type displays correctly in both views
3. Electricity data displays with proper formatting
4. Foretag shows "(exkl. moms)" note
5. Solar percentages calculate correctly
</verification>

<success_criteria>
- CUST-04: Customer type visible in calculation summary and public view
- Customer type in expanded details, not main summary area
- Electricity data (kopt el, price) visible where appropriate
- Solar data shows self-consumption percentages
- Foretag customers see VAT exclusion note
- Human verification confirms end-to-end flow works
</success_criteria>

<output>
After completion, create `.planning/phases/15-customer-electricity/15-07-SUMMARY.md`
</output>
