---
phase: 11-peak-calculation-engine
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/calculations/wizard/steps/consumption-profile-step.tsx
  - src/components/calculations/wizard/steps/results-step.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can input target average peak (kW) in wizard"
    - "User can input monthly peak ceiling (kW) in wizard"
    - "Results page uses user's target peak values in calculations"
    - "When target peak is null, system uses estimation or shows appropriate fallback"
  artifacts:
    - path: "src/components/calculations/wizard/steps/consumption-profile-step.tsx"
      provides: "Renders PeakTargetInput component"
      contains: "PeakTargetInput"
    - path: "src/components/calculations/wizard/steps/results-step.tsx"
      provides: "Uses targetAveragePeakKw from store"
      contains: "targetAveragePeakKw"
  key_links:
    - from: "consumption-profile-step.tsx"
      to: "peak-target-input.tsx"
      via: "import and render"
      pattern: "import.*PeakTargetInput"
    - from: "results-step.tsx"
      to: "calculation-wizard-store.ts"
      via: "useCalculationWizardStore"
      pattern: "targetAveragePeakKw"
---

<objective>
Wire orphaned PeakTargetInput component into wizard UI and connect user's target peak values to calculation engine.

Purpose: Close VERIFICATION.md gaps - the PeakTargetInput component (280 lines, fully functional) was created in 11-02 but never integrated. Similarly, results-step.tsx ignores user input and uses hardcoded DEFAULT_CURRENT_PEAK_KW. This plan completes the PEAK-05 and PEAK-06 user input flow.

Output: User can input peak targets in consumption profile step, and those values flow through to calculation results.
</objective>

<execution_context>
@/Users/julian.nordgren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julian.nordgren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-peak-calculation-engine/11-VERIFICATION.md

# Existing implementations to wire up
@src/components/calculations/controls/peak-target-input.tsx
@src/stores/calculation-wizard-store.ts
@src/components/calculations/wizard/steps/consumption-profile-step.tsx
@src/components/calculations/wizard/steps/results-step.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate PeakTargetInput into consumption profile step</name>
  <files>src/components/calculations/wizard/steps/consumption-profile-step.tsx</files>
  <action>
Import and render the PeakTargetInput component in the consumption profile step.

1. Add import at top of file:
   ```typescript
   import { PeakTargetInput } from '@/components/calculations/controls/peak-target-input'
   ```

2. Render PeakTargetInput in the left column (inputs area), AFTER the EstimationHelper component.
   The component depends on heatingType being selected, so placing it after existing inputs is correct.

   ```tsx
   {/* Peak target inputs - Phase 11 */}
   <PeakTargetInput />
   ```

3. The PeakTargetInput component already:
   - Shows placeholder when heatingType is null
   - Auto-estimates from annualConsumptionKwh and heatingType
   - Allows manual override
   - Persists to wizard store via zustand

No additional props needed - it reads all values from useCalculationWizardStore.
  </action>
  <verify>
1. Run `npm run build` - should compile without errors
2. Visual check: PeakTargetInput appears below EstimationHelper in consumption profile step
3. Functional check: Selecting a heating type triggers auto-estimate display
  </verify>
  <done>
PeakTargetInput component renders in consumption profile step.
User can see and modify target peak values in the wizard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire target peak values to calculation engine in results step</name>
  <files>src/components/calculations/wizard/steps/results-step.tsx</files>
  <action>
Replace hardcoded DEFAULT_CURRENT_PEAK_KW with user's target peak from wizard store.

1. Add targetAveragePeakKw to the destructured store values (around line 58-73):
   ```typescript
   const {
     // ... existing fields
     // Phase 11: Peak targets
     targetAveragePeakKw,
   } = useCalculationWizardStore()
   ```

2. In the calculateBatteryROI call (around line 124), replace:
   ```typescript
   currentPeakKw: DEFAULT_CURRENT_PEAK_KW,
   ```
   with:
   ```typescript
   currentPeakKw: targetAveragePeakKw ?? DEFAULT_CURRENT_PEAK_KW,
   ```

3. In the PeakShavingSlider props (around line 213), replace:
   ```typescript
   currentPeakKw={DEFAULT_CURRENT_PEAK_KW}
   ```
   with:
   ```typescript
   currentPeakKw={targetAveragePeakKw ?? DEFAULT_CURRENT_PEAK_KW}
   ```

4. DEFAULT_CURRENT_PEAK_KW import can remain - it's the fallback when user hasn't entered a value.

The null coalescing (??) operator provides the correct fallback behavior:
- If user entered a value -> use their value
- If targetAveragePeakKw is null -> fall back to DEFAULT_CURRENT_PEAK_KW (15 kW)

This ensures backwards compatibility while enabling the new peak input flow.
  </action>
  <verify>
1. Run `npm run build` - should compile without errors
2. Run `npm run lint` - no linting errors
3. Functional test flow:
   - Create new calculation wizard
   - Enter customer info, consumption profile with heating type
   - Observe PeakTargetInput auto-estimate appears
   - Proceed to results step
   - Verify PeakComparison shows user's target peak value, not hardcoded 15 kW
  </verify>
  <done>
targetAveragePeakKw from wizard store flows through to calculateBatteryROI.
Results page shows peak comparison using user's actual input values.
DEFAULT_CURRENT_PEAK_KW only used as fallback when no user input exists.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Build verification:**
   ```bash
   npm run build
   ```
   Should complete without errors.

2. **Type verification:**
   ```bash
   npm run typecheck
   ```
   Should pass with no type errors.

3. **Linting:**
   ```bash
   npm run lint
   ```
   Should pass with no errors.

4. **End-to-end flow verification:**
   - Start dev server: `npm run dev`
   - Navigate to calculation wizard
   - Complete customer info step
   - In consumption profile step:
     - Select heating type (e.g., "Luft-luft varmepump")
     - Observe PeakTargetInput appears with auto-estimated values
     - Optionally modify values manually
   - Proceed through battery selection to results
   - Verify PeakComparison component shows the target peak you entered (not 15 kW default)

5. **Regression check:**
   - If no heating type selected, PeakTargetInput shows placeholder message
   - If targetAveragePeakKw is null (legacy calculations), fallback to DEFAULT_CURRENT_PEAK_KW works
</verification>

<success_criteria>
- PeakTargetInput component renders in consumption-profile-step.tsx
- targetAveragePeakKw is read from wizard store in results-step.tsx
- calculateBatteryROI receives user's target peak value
- PeakShavingSlider uses user's target peak value
- Fallback to DEFAULT_CURRENT_PEAK_KW works when no user input
- All builds pass, no type errors, no lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-peak-calculation-engine/11-04-SUMMARY.md`
</output>
